# Remote state backend (recommended before sharing or running CI).
#
# 1. Create the bucket + lock table once (one-time bootstrap):
#
#    aws s3api create-bucket --bucket my-platform-obs-tfstate \
#      --region ap-south-1 --create-bucket-configuration LocationConstraint=ap-south-1
#    aws s3api put-bucket-versioning --bucket my-platform-obs-tfstate \
#      --versioning-configuration Status=Enabled
#    aws dynamodb create-table --table-name platform-obs-tflock \
#      --attribute-definitions AttributeName=LockID,AttributeType=S \
#      --key-schema AttributeName=LockID,KeyType=HASH \
#      --billing-mode PAY_PER_REQUEST --region ap-south-1
#
# 2. Uncomment the `backend "s3" {}` block in versions.tf.
# 3. Rename this file to backend.tf and run `terraform init -migrate-state`.

terraform {
  backend "s3" {
    bucket         = "my-platform-obs-tfstate"
    key            = "platform-observability-stack/dev/terraform.tfstate"
    region         = "ap-south-1"
    dynamodb_table = "platform-obs-tflock"
    encrypt        = true
  }
}
